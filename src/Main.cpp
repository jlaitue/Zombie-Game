#include "../inc/Game.hpp"
#include "../inc/Board.hpp"
#include <ncurses.h>

int main() {
  // initscr();
  // raw();
  // clear();
  //
  // mvaddstr(LINES-1, 0, "PRESS ANY KEY TO EXIT... ");
  // refresh();
  // getch();
  Board board;
  // Game game;
  // game.run();
  // board.displayBoard();
  // endwin();
  // exit(0);
  return 0;
}
